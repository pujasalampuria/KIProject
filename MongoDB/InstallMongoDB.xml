<KnowledgeItem ID="OpexSoftware:WorkFlowUserStory:InstallMongoDB" xmlns="http://www.arago.de/KiSchema">
	<Title>InstallMongoDB</Title>
	<Description>Initiates the workflow for installing mongoDB</Description>
	<BindAttributes>
		<AttributeFilter Name="NodeType" Mode="string" Value="Machine"/>
	</BindAttributes>

	<Log>__KI ${KIID} is now bound to ${NODE:NodeID}__</Log>

	<CreateTrigger>
	     <IssueCondition>
		 <Description>Searches for an Issue with DataBlock INSTALLMONGODB and triggers the workflow for sucessful completion</Description>
			<SubItem Name="INSTALLMONGODB" Alias="IMD">
				<AttributeFilter Name="TargetMachine" Mode="string" Value="${NODE:NodeName}"/>
				<AttributeFilter Name="TargetState" Mode="string" Value="Created"/>
				<AttributeFilter Name="Status" Mode="notexists"/>
				<AttributeFilter Name="TargetApp" Mode="exists"/>
			</SubItem>
		</IssueCondition>

	     <Activate>
			<SetAttribute Name="IMD:Status" Value="WorkflowStarted" /> 
			<GenerateData Alias="PROTASKS">
                            <DataType>ProcessingTasks</DataType>
                            <CurrentTask>1</CurrentTask>
			</GenerateData>

			<!--Task1 Check if MongoDB package is there -->
			
			<IncAttribute Name="PROTASKS:NumberTotal"/>
			
			<XMLExtract StoreTo="NODE:Extensions" Mode="simple"><![CDATA[<NewBlock><Tasks
				TargetMachine=${IMD:TargetMachine}      
				TargetState=Created
				User=${IMD:User}
				Lib=APTSOURCEMODIFICATION
				PKG=mongodb-org
				OSSpecific=Y
				Host=${IMD:Host}
				AptRepoUrl='${IMD:AptRepoUrl}'
				AptArchiveType=${IMD:AptArchiveType}
				AptDistribution='${IMD:AptDistribution}'
				AptComponents=${IMD:AptComponents}
				AptKey=${IMD:AptKey}
				CurrentState=ReadState
				TaskNumber=${PROTASKS:NumberTotal}
				/> </NewBlock>]]>
			</XMLExtract>
			
      
			<GenerateData Alias="VAL"> 
				<!-- Independent="true" can be applied, if multiple Issues should be generated. In this case pass on the parent IssueID to the resulting Issue <ParentIID>${IID}</ParentIID>, don't forget to include ParentIID=${IID} into the Tasks DataBlock on the Node -->
				<DataType>Validate</DataType>
				<ExtUID>${NODE:Extensions/Tasks/UID}</ExtUID>
			</GenerateData>	

			<!--Task2 Check if MongoDB package is there -->	
  
            <IncAttribute Name="PROTASKS:NumberTotal"/>

			<XMLExtract StoreTo="NODE:Extensions" Mode="simple"><![CDATA[<NewBlock><Tasks
				TargetMachine=${IMD:TargetMachine}      
				TargetState=Created
				User=${IMD:User}
				Lib=PKG
				OSSpecific=Y
				PKG=mongodb-org
				Host=${IMD:Host}
				CurrentState=ReadState
				TaskNumber=${PROTASKS:NumberTotal}
				/> </NewBlock>]]>
			</XMLExtract>
			
      
			<GenerateData Alias="VAL"> 
				<!-- Independent="true" can be applied, if multiple Issues should be generated. In this case pass on the parent IssueID to the resulting Issue <ParentIID>${IID}</ParentIID>, don't forget to include ParentIID=${IID} into the Tasks DataBlock on the Node -->
				<DataType>Validate</DataType>
				<ExtUID>${NODE:Extensions/Tasks/UID}</ExtUID>
			</GenerateData>	
      
			<!--<Resolve>Resolved initial Issue with ID ${IID}</Resolve> - use if ChildIssues are created-->
			
			<GenerateData> 
				<DataType>Resolve</DataType>
				<Status>checkForResolve</Status>
			</GenerateData>	
		</Activate>
	</CreateTrigger>
</KnowledgeItem>
