<KnowledgeItem ID="OpexSoftware:WorkFlowUserStory:RemoveSudoUser" xmlns="http://www.arago.de/KiSchema">
	<Title>RemoveSudoUser</Title>
	<Description>Initiates the workflow for removing a user from sudoers list.
	</Description>
	<BindAttributes>
		<AttributeFilter Name="NodeType" Mode="string" Value="Machine" />
	</BindAttributes>

	<Log>__KI ${KIID} is now bound to ${NODE:NodeID}__</Log>

	<CreateTrigger>
		<IssueCondition>
			<Description>Searches for an Issue with DataBlock REMOVESUDOUSER and triggers the workflow for successful completion</Description>
			<SubItem Name="REMOVESUDOUSER" Alias="RSU">
				<AttributeFilter Name="TargetMachine" Mode="string" Value="${NODE:NodeName}" />
				<AttributeFilter Name="TargetState" Mode="string" Value="Created" />
				<AttributeFilter Name="Status" Mode="notexists" />
			    <AttributeFilter Name="LinuxUser" Mode="exists" />
			</SubItem>
		</IssueCondition>

		<Activate>
			<SetAttribute Name="RSU:Status" Value="WorkflowStarted" />
			<GenerateData Alias="PROTASKS">
				<DataType>ProcessingTasks</DataType>
				<CurrentTask>1</CurrentTask>
			</GenerateData>

			<!--Task Remove user from sudoers list. -->

			<IncAttribute Name="PROTASKS:NumberTotal" />

			<XMLExtract StoreTo="NODE:Extensions" Mode="simple"><![CDATA[<NewBlock><Tasks
				TargetPath='${RSU:TargetPath}'
				TargetMachine=${RSU:TargetMachine}
				TargetState=Created
				User=${RSU:User}
				Host=${RSU:Host}
				LinuxUser=${RSU:LinuxUser}
				Lib=RUNCOMMAND
				Command='sudo deluser ${RSU:LinuxUser} sudo'
				CurrentState=ReadState
                TaskNumber=${PROTASKS:NumberTotal}
				/></NewBlock>]]>
			</XMLExtract>

			<GenerateData Alias="VAL">
				<!-- Independent="true" can be applied, if multiple Issues should be 
					generated. In this case pass on the parent IssueID to the resulting Issue 
					<ParentIID>${IID}</ParentIID>, don't forget to include ParentIID=${IID} into 
					the Tasks DataBlock on the Node -->
				<DataType>Validate</DataType>
				<ExtUID>${NODE:Extensions/Tasks/UID}</ExtUID>
			</GenerateData>

			<!--<Resolve>Resolved initial Issue with ID ${IID}</Resolve> - use if 
				ChildIssues are created -->

			<GenerateData>
				<DataType>Resolve</DataType>
				<Status>checkForResolve</Status>
			</GenerateData>
		</Activate>
	</CreateTrigger>
</KnowledgeItem>
